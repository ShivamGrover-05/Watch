# AURELIA ATELIER Environment Configuration
# No mandatory secret environment variables required for basic deployment.

# Optional: Only needed if deploying to a subpath repository on GitHub Pages (e.g. /Watch).
# On Vercel, leave empty so the application is served at the domain root (/).
NEXT_PUBLIC_BASE_PATH=
